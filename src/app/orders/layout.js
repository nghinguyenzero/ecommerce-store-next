export const metadata = {
  title: "My Orders",
  description: "Track and manage your orders at Zero Store.",
  robots: { index: false, follow: false },
};

export default function OrdersLayout({ children }) {
  return children;
}
