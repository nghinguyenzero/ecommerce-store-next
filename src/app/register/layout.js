export const metadata = {
  title: "Create Account",
  description: "Join Zero Store today. Create your account to start shopping the latest fashion trends.",
};

export default function RegisterLayout({ children }) {
  return children;
}
