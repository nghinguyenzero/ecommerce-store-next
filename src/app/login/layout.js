export const metadata = {
  title: "Login",
  description: "Sign in to your Zero Store account to manage orders, cart and profile.",
};

export default function LoginLayout({ children }) {
  return children;
}
