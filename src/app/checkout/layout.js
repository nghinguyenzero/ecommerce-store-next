export const metadata = {
  title: "Checkout",
  description: "Complete your purchase at Zero Store. Secure payment with Stripe.",
  robots: { index: false, follow: false },
};

export default function CheckoutLayout({ children }) {
  return children;
}
